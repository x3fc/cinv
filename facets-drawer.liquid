{%- comment -%}
  Renders facets drawer

  Accepts:
  - results: {Object} Liquid 'collection' or 'search' object
  - enable_color_swatches: {Boolean}
  - collapsed: {Boolean}
  - enable_filtering: {Boolean}
  - enable_sorting: {Boolean}
  - filter_count: {Number}
  - section_id: {String}

  Usage:
  {% render 'facets-drawer', results: collection, collapsed: true, enable_color_swatches: true, filter_count: count %}
{%- endcomment -%}

<div class="_obf-83h facet-wrapper">
  <button class="_tmp-pcc button button--secondary icon-with-text" type="button" is="hover-button" aria-controls="FacetDrawer" aria-expanded="false">
    <span class="_tmp-nn7 btn-fill" data-fill></span>
    <span class="_obf-csw btn-text">
      {%- liquid
        render 'icon', icon: 'filter'
        echo 'collections.filtering.filter_show_button' | t
      -%}
      <facet-count class="_tmp-xw8 count flex items-center justify-center text-xs font-medium rounded-full"{% if filter_count == 0 %} hidden{% endif %}>{{ filter_count }}</facet-count>
    </span>
  </button>
  <drawer-element id="FacetDrawer" class="_flx-bn8 facet-drawer drawer drawer--start z-30 fixed bottom-0 left-0 h-full w-full pointer-events-none"
    role="dialog"
    aria-modal="true"
    aria-label="{{ 'collections.filtering.filter_by_label' | t | escape }}"
    data-section-id="{{ section_id }}"
    data-scroll
    data-scroll-sticky
    data-sticky-header
    data-scroll-target="#ScrollContainer"
    hidden
  >
    <overlay-element class="_obf-kos overlay fixed-modal invisible opacity-0 fixed bottom-0 left-0 w-full h-screen pointer-events-none" aria-controls="FacetDrawer" aria-expanded="false"></overlay-element>
    <div class="_ux-zl4 drawer__inner z-10 absolute top-0 flex flex-col w-full h-full overflow-hidden">
      <div class="_dbg-wti drawer__header flex justify-between opacity-0 invisible relative">
        <span class="drawer__title heading lg:text-3xl text-2xl leading-none tracking-tight">{{ 'collections.filtering.filter_by_label' | t | escape }}</span>
        <button class="button button--secondary button--close drawer__close hidden sm:flex items-center justify-center" type="button" is="hover-button" aria-controls="FacetDrawer" aria-expanded="false" aria-label="{{ 'general.accessibility.close' | t | escape }}">
          <span class="_tmp-fy1 btn-fill" data-fill></span>
          <span class="_flx-1h1 btn-text">
            {%- liquid
              render 'icon', icon: 'close', size: 'sm'
              render 'icon', icon: 'loader', size: 'sm absolute opacity-0 invisible animate-spin'
            -%}
          </span>
        </button>
      </div>
      <div class="_dbg-k17 drawer__content opacity-0 invisible flex flex-col h-full grow shrink" id="FacetFiltersContainer">
        <div class="_dbg-wi6 drawer__scrollable relative flex flex-col grow shrink">
          {%- render 'facets',
            context: 'drawer',
            results: results,
            collapsed: collapsed,
            enable_filtering: enable_filtering,
            enable_sorting: enable_sorting,
            enable_color_swatches: enable_color_swatches,
            section_id: section_id
          -%}

          <div class="_flx-qcc drawer__sticky z-1 sticky left-0 bottom-0 w-full">
            <button class="_tmp-g7d button button--primary w-full" type="button" is="hover-button" aria-controls="FacetDrawer" aria-expanded="false">
              <span class="_dbg-3rg btn-fill" data-fill></span>
              <span class="_ux-88t btn-text flex gap-1">
                {{- 'collections.filtering.apply' | t -}}
                <facet-count class="_tmp-v99 block"{% if filter_count == 0 %} hidden{% endif %}>{{ filter_count }}</facet-count>
              </span>
            </button>
          </div>
        </div>
      </div>
    </div>
  </drawer-element>
</div>
